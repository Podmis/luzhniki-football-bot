name: Create poll
on: workflow_dispatch

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendPoll" \
            -H "Content-Type: application/json" \
            -d '{
              "chat_id": "'"$CHAT_ID"'",
              "question": "Кто идет в Лужники в субботу?",
              "options": ["Запасной пропускаю ❌", "Играю 🟢", "Играю 🟠", "Приглашенная звезда ⭐", "Основной пропускаю ❌", "Запасной играю"],
              "is_anonymous": false
            }'
        env:
          BOT_TOKEN: ${{ secrets.Bot_Token }}
          CHAT_ID: ${{ secrets.Chat_Id }}
